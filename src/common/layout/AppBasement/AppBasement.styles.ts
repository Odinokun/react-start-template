import { styled, Container, Box } from '@mui/material';

export const AppFooter = styled(Box)`
  position: absolute;
  left: 0;
  bottom: 0;
  width: 100%;
`;

export const AppContainer = styled(Container)`
  display: flex;
  justify-content: center;
  align-items: center;
  padding: 10px 0;
`;
